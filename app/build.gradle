apply plugin: 'com.android.application'

android {
    compileSdkVersion 20
    buildToolsVersion '20.0.0'
    defaultConfig {
        applicationId 'gr.sv1jsb.kratascore'
        minSdkVersion 15
        targetSdkVersion 20
        versionCode 6
        versionName '1.5'
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile project(':cropimage')
}
